<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.raftls.running">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Running"
        android:usesCleartextTraffic="true"
        tools:targetApi="m"
        tools:replace="android:icon">
        <activity android:name=".tracking.ui.TrackingActivity" />
        <activity android:name=".app.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentification.activities.LoginActivity" />
        <activity android:name=".authentification.activities.RegisterActivity" />
        <activity android:name=".app.activities.MainActivity" />
    </application>

</manifest>